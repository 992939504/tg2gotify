# 机密文件，永不入库
# 注意：config.json.* 覆盖 .bak / .bak-时间戳 / .tmp 等所有派生文件（含 API_HASH、Gotify token）
config.json
config.json.*
*.session
*.session-journal

# 运行环境
venv/
__pycache__/
*.pyc

# 运行日志与运行时状态（含账号名、账号 id、频道 id，不入库）
*.log
login_state.json
login_state.json.tmp
run_status.json
run_status.json.tmp
